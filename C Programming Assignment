Q-1    What do you mean by arrays give one example of array and how you can declared an arreay ?
ANS - #include <stdio.h>

int main() {
    int numbers[] = {15, 25, 35, 45, 55};  // Declaration and initialization of an array

    // Accessing elements
    printf("First element: %d\n", numbers[0]); // Index starts from 0
    printf("Third element: %d\n", numbers[3]);

    return 0;
}

OUTPUT
First element: 15
Third element: 45

Q-2  What do you mean by functions ? Write the type of funtions and give one example in code of each type.
 ANS - #include <stdio.h>
#include <math.h>

int main() {
    double num = 16.0;
    double result = sqrt(num);  // Using library function sqrt()
    printf("Square root of %.2f is %.2f\n", num, result);
    return 0;
}

OUTPUT 
Square root of 36.00 is 6.00

Q-3 What do you mean by pointers ? Write a code how you can initialise the pointer.
 ANS- #include <stdio.h>

int main() {
    int num = 15;       // A normal integer variable
    int *ptr;           // Declaration of a pointer to an integer
    ptr = &num;         // Initialize pointer with the address of num

    printf("Value of num: %d\n", num);          // prints 15
    printf("Address of num: %p\n", &num);       // prints memory address
    printf("Value stored in ptr: %p\n", ptr);   // same address as &num
    printf("Value pointed by ptr: %d\n", *ptr); // prints 15 (value at address)

    return 0;
}


OUTPUT
Value of num: 15
Address of num: 0x7ffc26d81a24
Value stored in ptr: 0x7ffc26d81a24
Value pointed by ptr: 15

Q-4 What are the types of array ? Write in a code 1D & 2D included.
ANS - int main() {
    int marks[5] = {80, 85, 90, 75, 95};  // 1D array declaration
    int i;

    printf("Student Marks:\n");
    for(i = 0; i < 5; i++) {
        printf("%d ", marks[i]);
    }

    return 0;
}
//   Output - Student Marks: 80 85 90 75 95

// 2. Two-Dimensional Array Example:
     #include <stdio.h>
int main() {
    int matrix[2][3] = {{1, 2, 3}, {4, 5, 6}};  // 2D array declaration
    int i, j;

    printf("Matrix Elements:\n");
    for(i = 0; i < 2; i++) {
        for(j = 0; j < 3; j++) {
            printf("%d ", matrix[i][j]);
        }
        printf("\n");
    }

    return 0;
}
    // Output - Matrix Elements:
              // 1 2 3  
             // 4 5 6

Q-5 Explain the type of Operators in code
 ANS - #include <stdio.h>
int main() {
    int a = 10, b = 5;

    // Arithmetic Operators
    printf("Addition: %d\n", a + b);
    printf("Subtraction: %d\n", a - b);

    // Relational Operators
    printf("Is a greater than b? %d\n", a > b);

    // Logical Operators
    printf("Logical AND (a>5 && b<10): %d\n", (a > 5 && b < 10));

    // Assignment Operators
    a += 5;  // same as a = a + 5
    printf("After a += 5, a = %d\n", a);

    // Increment/Decrement Operators
    a++;
    b--;
    printf("After increment a=%d, after decrement b=%d\n", a, b);

    return 0;
}
OUTPUT
Addition: 15
Subtraction: 5
Is a greater than b? 1
Logical AND (a>5 && b<10): 1
After a += 5, a = 15
After increment a=16, after decrement b=4
